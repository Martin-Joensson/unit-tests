const months = [
  'January', 'February', 'March', 'April', 'May', 'June',
  'July', 'August', 'September', 'October', 'November', 'December'
]

export const monthName = (monthNumber) => {
const monthName = months[monthNumber-1]
  return monthName ? monthName : null;
}
